// import mongoose, { model, Schema } from "mongoose";

// // export const UserModel = model("ariguun", new Schema({ age: String }));
// export const UserModel = mongoose.model(
//   "COLLECTIONS",
//   new Schema({
//     FOOD: String,
//     OREDER:
//     FOOD:
//     CATEGORY
//   })
// );
