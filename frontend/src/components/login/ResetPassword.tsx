import { Box, Typography } from "@mui/material";

export const ResetPassword = () => {
  return (
    <Box sx={{ display: "flex", gap: "48px" }}>
      <Typography>Нууц үг сэргээх</Typography>
      <Box sx={{ display: "flex", gap: 2, flexDirection: "column" }}></Box>
    </Box>
  );
};
